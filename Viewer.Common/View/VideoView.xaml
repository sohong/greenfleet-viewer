<UserControl x:Class="Viewer.Common.View.VideoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="451" d:DesignWidth="638">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <MediaElement Name="mediaMain"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="3">
            <Button Margin="2" Padding="2">Play</Button>
            <Button Margin="2" Padding="2">Stop</Button>
            <Button Margin="2" Padding="2">Pause</Button>
            <Button Margin="2" Padding="2">Home</Button>
            <Button Margin="2" Padding="2">Previous</Button>
            <Button Margin="2" Padding="2">Next</Button>
            <Button Margin="2" Padding="2">End</Button>
        </StackPanel>
    </Grid>
</UserControl>
